server:
  port: 8080

spring:
  application:
    name: ms-admin-api-gateway
  cloud:
    gateway:
      # Descubrimiento automatico de servicios registrados en Eureka
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
      # Rutas explicitas hacia cada microservicio (resueltas por Eureka con lb://)
      routes:
        - id: ms-auth
          uri: lb://ms-auth
          predicates:
            - Path=/api/auth/**
        - id: ms-gestion-instructor
          uri: lb://ms-gestion-instructor
          predicates:
            - Path=/api/instructores/**
        - id: ms-gestion-alumno
          uri: lb://ms-gestion-alumno
          predicates:
            - Path=/api/alumnos/**
        - id: ms-gestion-taller
          uri: lb://ms-gestion-taller
          predicates:
            - Path=/api/talleres/**
      # CORS centralizado para el cliente web (Unidad 3)
      globalcors:
        cors-configurations:
          '[/**]':
            allowed-origin-patterns: "*"
            allowed-methods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowed-headers: "*"
            allow-credentials: true

eureka:
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
    instance-id: localhost:${spring.application.name}:${server.port}
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
