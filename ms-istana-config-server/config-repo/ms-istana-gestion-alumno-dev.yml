server:
  port: 8082

spring:
  application:
    name: ms-istana-gestion-alumno
  datasource:
    url: jdbc:postgresql://localhost:5434/alumnodb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
