server:
  port: 8081

spring:
  application:
    name: ms-istana-gestion-instructor
  datasource:
    url: jdbc:postgresql://localhost:5433/instructordb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
