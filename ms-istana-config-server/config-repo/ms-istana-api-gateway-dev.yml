server:
  port: 8080

spring:
  application:
    name: ms-istana-api-gateway

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info
